# OpenAI 兼容 API 密钥（用于星火 X1 等服务）
# 格式：APIpassword（具体值请参考服务商文档）
OPENAI_API_KEY=

# 语音识别 IAT 配置（仅供参考，真实值请设置在 .env.local）
IAT_API_KEY=your_iat_api_key_here      # 语音识别 APIKey（仅本地 .env.local 使用）
IAT_API_SECRET=your_iat_api_secret_here # 语音识别 APISecret（仅本地 .env.local 使用）
IAT_APP_ID=your_iat_app_id_here        # 可选，部分接口需要 APPID
IAT_TIMEOUT_MS=20000                   # 可选，请求超时时间（毫秒），默认 20000
IAT_LOG_LEVEL=info                     # 可选，日志级别：info|error|silent
